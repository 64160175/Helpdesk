<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link rel="icon" type="image/png" href="/img/icon.png">
    <title>Login</title>
    <link href="/css/style.css" rel="stylesheet">

    <!-- ฟอนต์ Prompt -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>

<body class="bg-primary prompt-regular animated-gradient">
    <div id="layoutAuthentication">
        <div id="layoutAuthentication_content">
            <main>
                <div class="container">
                    <div class="row justify-content-center ">
                        <div class="col-lg-5">
                            <br><br><br>
                            <h1 class="display-5 text-center mt-4 text-white ">ระบบเบิกอุปกรณ์ไอที</h1>

                            <div class="card shadow-lg border-0 rounded-lg mt-5">
                                <div class="card-header">
                                    <h3 class="text-center my-2 prompt-regular">เข้าสู่ระบบ</h3>
                                </div>
                                <div class="card-body">
                                    <form action="/login" method="POST">
                                        <div class="form-floating mb-3">
                                            <input class="form-control prompt-light" id="inputusername" name="username"
                                                type="text" placeholder="ชื่อผู้ใช้" required />
                                            <label for="inputusername" class="prompt-light">ชื่อผู้ใช้</label>
                                        </div>
                                        <div class="form-floating mb-3">
                                            <input class="form-control prompt-light" id="inputPassword" name="password"
                                                type="password" placeholder="รหัสผ่าน" required />
                                            <label for="inputPassword" class="prompt-light">รหัสผ่าน</label>
                                        </div>
                                        <div class="text-center">
                                            <button type="submit"
                                                class="btn btn-primary prompt-medium">เข้าสู่ระบบ</button>
                                        </div>
                                        <% if (typeof error !=='undefined' && error) { %>
                                            <div class="text-danger mt-2 text-center">
                                                <%= error %>
                                            </div>
                                            <% } %>
                                    </form>
                                </div>
                            </div>

                            <p class="mt-4 text-center prompt-light text-white">หมายเหตุ : มาเบิกได้เฉพาะวัน จ.-ส. เวลา
                                09.00 -
                                16.00</p>
                            <p class="text-center">
                                <a href="/guide/guide_user.pdf" target="_blank" class=" prompt-light">
                                    <i class="fas fa-book"></i> คู่มือการใช้งาน
                                </a>
                            </p>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    </div>
</body>

</html>